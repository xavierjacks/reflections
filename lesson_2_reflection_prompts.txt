What happens when you initialize a repository? You initialize an empty directory. Why do you need to do it? To begin your version control.

How is the staging area different from the working directory and the repository? The staging area is where you bundle the files together into one commit for the repository.
What value do you think it offers? It compresses the size of the commit.

How can you use the staging area to make sure you have one commit per logical
change?

What are some situations when branches would be helpful in keeping your history
organized? How would branches help? Branches are helpful when multiple people are working on a project or when experimentation is needed. Branches allow a change to be made while not changing the final project.

How do the diagrams help you visualize the branch structure? It helps see how one commit effects/relates to another.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do? Merging branches into a single file removes deleted versions and adds new version of a project.

What are the pros and cons of Git’s automatic merging vs. always doing merges
manually? Again it allows for experimentation however convenience is helpful.
